@page
@model TaskTracker.Pages.Dashboard.IndexModel
@using TaskTrackerDAL.Constants
@{
    Layout = "_Layout";
}

@if (User.IsInRole(AppRoles.Admin))
{
    <div class="container mt-xxl-5">
        <h2>Admin Dashboard</h2>
        <hr />

        <div class="row">

            <div class="col-md-3">
                <div class="card p-3">
                    <h5>Total Companies</h5>
                    <h2>@Model.AdminDashboard?.TotalCompanies</h2>
                </div>
            </div>

            <div class="col-md-3">
                <div class="card p-3">
                    <h5>Total Users</h5>
                    <h2>@Model.AdminDashboard?.TotalUsers</h2>
                </div>
            </div>

            <div class="col-md-3">
                <div class="card p-3">
                    <h5>Total Projects</h5>
                    <h2>@Model.AdminDashboard?.TotalProjects</h2>
                </div>
            </div>

            <div class="col-md-3">
                <div class="card p-3">
                    <h5>Total Tasks</h5>
                    <h2>@Model.AdminDashboard?.TotalTasks</h2>
                </div>
            </div>

        </div>
    </div>

}